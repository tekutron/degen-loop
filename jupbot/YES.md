One-tap approvals

- Buy: `YES <proposalId>`
  - Must be a proposalId that still exists in `trade_proposals.json` (the loop refreshes and old ids rotate out).

- If you reply `YES` without an id, nothing will happen.

- Sell: auto-sell triggers on TP/SL when AUTO_SELL=1.
